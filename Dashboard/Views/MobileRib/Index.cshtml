@using Dashboard.Utils
@{
    ViewBag.Title = "Mobile Onboarding With RIB";
    List<MobileRibStateSummary> mobileRibStateSummaries = ViewBag.MobileRib;
}
@section featured {
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>@ViewBag.Title</h1>
                <h2></h2>
            </hgroup>
        </div>
    </section>
}
<div class="container">
    <div class="row">
        <h4>
            @{ var today = DateTime.Now.ToString("dd-MMM-yyyy");}
            Date: @Html.DisplayFor(modelItem => today)
        </h4>
    </div>

    <div class="row">
        <div class="col-sm-12">
            <div>
                <h3 class="text-primary">Mobile Onboarding With RIB</h3>
                <table class="table table-striped table-condensed table-bordered">
                    <thead>
                        <tr>
                            <th>Date Generated</th>
                            <th>Username</th>
                            <th>Response</th>
                            <th>State Indicator</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var summary in mobileRibStateSummaries)
                        {
                            <tr>
                                <td class="col-sm-2"><b>@Html.DisplayFor(modelItem => summary.DateGenerated)</b></td>
                                <td class="col-sm-2 text-warning text-right"><b>@Html.DisplayFor(modelItem => summary.Username)</b></td>
                                <td class="col-sm-2 text-warning text-right"><b>@Html.DisplayFor(modelItem => summary.InitialPayload)</b></td>
                                <td class="col-sm-2 text-success"><b>@Html.DisplayFor(modelItem => summary.TransactionStateInd)</b></td>
                            </tr>
                        }
                    </tbody>

                </table>
            </div>
        </div>

    </div>
</div>

@section scripts
{
    <script>
        function refresh() {
            setTimeout(function () {
                location.reload();
            }, 120000);
        }
        refresh();
    </script>
}